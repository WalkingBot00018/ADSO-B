class Conductor: #Se crea una palabra reservada class con el nombre 'Conductor' y se cierra con dos puntos para indicar que inica el bloque de codigo de la misma.
    def __init__(self,nombre,documento): #Se instancia o se crea el constructor de la clase Conductor que contiene tres parametros en su lista de parametros, el primero que es self que es el parametro obligatorio para indicar que el constructor esta dentro de la clase, el segundo parametro es nombre y el tercer parametro es documento.
        self.__nombre=nombre #Se crea el primer atributo para la clase Conductor que es self.__nombre el cual contendra lo que contenga el parametro nombre cuando se instancie un objeto de la clase Conductor.
        self.__documento=documento #Se crea el segundo atributo para la clase Conductor que es self.__documento el cual contendra lo que contenga el parametro documento cuando se instancie un objeto de la clase Conductor.
    def getNombre(self): #Se crea el primer metodo para la clase Conductor que se llamma getNombre el cual en su lista de parametros contiene el parametro self, que es un parametro obligatorio para indicar que el aprametro se encuentra dentro de la clase Conductor.
        return self.__nombre #El return servira para retornar y mostrar en consola basicamente que contiene el atributo self.__nombre para poder manipularlo fuera del objeto.
    def getDocumento(self): #Se crea el segundo metodo llamado getDocumento() que servira para mostrar por consola cuando se llame lo que contiene el atributo self.__documento, en su lista de parametros se encuentra self, que sirve para indicar que que el metodo se encuentra dentro de la clase.
        return self.__documento #El return servira para retornar el valor o tipo de dato que contiene e atributo self.__documento el cual se mostrar en pantalla una vez que su metodo sea llamado.